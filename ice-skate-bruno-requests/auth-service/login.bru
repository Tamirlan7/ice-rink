meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{api_url}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "phoneNumber": "+77066651161",
    "password": "1954623790"
  }
}

script:post-response {
  if (res.body && res.body.accessToken) {
    bru.setEnvVar("bearer_token", res.body.accessToken)
  }
  
}
